/// The single source of sample truth — canned wire JSON for every documented PRD scenario, plus
/// DTO/domain factories reachable through one helper (`SampleData.jsonData(for:)` and the typed
/// accessors). Used by previews, tests, snapshots, and mock dependencies.
///
/// The accessor + factories arrive in TASK-002; this is the namespace stub.
public enum SampleData {}
