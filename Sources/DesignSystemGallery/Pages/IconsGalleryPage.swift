import DesignSystem
import SwiftUI

/// Design System → Icons. Every `Icon` registry entry as a labelled grid cell (glyph + name + role).
/// Content lands in TASK-002.
struct IconsGalleryPage: View {
  var body: some View {
    Text("Icons").navigationTitle("Icons")
  }
}
