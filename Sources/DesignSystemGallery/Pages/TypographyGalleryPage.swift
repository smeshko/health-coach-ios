import DesignSystem
import SwiftUI

/// Design System → Typography. Every `Font.coach…` token as a sample line with its name + role.
/// Content lands in TASK-002.
struct TypographyGalleryPage: View {
  var body: some View {
    Text("Typography").navigationTitle("Typography")
  }
}
